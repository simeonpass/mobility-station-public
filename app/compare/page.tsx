import type { Metadata } from "next";
import Link from "next/link";
import { displayPrice, gbp, getProducts } from "@/lib/catalog";
export const dynamic="force-dynamic";
export const metadata: Metadata={title:"Compare XSTO M4, M4B, M4 Pro, X12 & X12 Pro | KLYM",description:"Compare the five core XSTO powered mobility models available from KLYM Mobility."};
export default async function Compare(){const products=await getProducts();return <main className="page-shell"><section className="page-hero"><p className="eyebrow plain">XSTO COMPARISON</p><h1>Five models. Find your fit.</h1><p>Compare the KLYM XSTO range side by side, with pricing kept in sync with the Mobility Station V1 catalogue where a live product record exists.</p></section><section className="content-section"><div className="compare-wrap"><table className="compare-table"><thead><tr><th>Model</th><th>VAT-relief price</th><th>Best for</th><th>Core idea</th><th></th></tr></thead><tbody>{products.map(p=><tr key={p.shortName} className={p.shortName==="M4B"?"best":""}><td><strong>{p.shortName}</strong>{p.shortName==="M4B"&&<div><small>OUR PICK</small></div>}</td><td>{gbp(displayPrice(p))}</td><td>{p.shortName.startsWith("X12")?"All-terrain and advanced access":p.shortName==="M4 Pro"?"Premium everyday mobility":p.shortName==="M4B"?"Latest M4 evolution":"Compact self-balancing mobility"}</td><td>{p.description?.split(".")[0]}</td><td><Link className="text-link" href={`/${p.slug}`}>View →</Link></td></tr>)}</tbody></table></div></section></main>}
