import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { Breadcrumbs } from "@/components/layout/breadcrumbs";
import { AdaptationCard } from "@/components/product/adaptation-card";
import { MotabilityProductCard } from "@/components/product/motability-product-card";
import { ProductCard } from "@/components/ProductCard";
import { ProductDetailView } from "@/components/product/product-detail-view";
import { ProductReviews } from "@/components/product/product-reviews";
import { adaptationHref, findSectionForCategory, isAdaptationProduct, sectionHref } from "@/lib/adaptations";
import { getProductReviews } from "@/lib/data";
import { conditionGradeLabel, conditionLabel, displayPrice, formatGBP, getAllPublishedSlugs, getProductBySlug, getRelatedProducts, isUsedCondition, primaryImage, stockStatus } from "@/lib/products";
import { jsonLdScript, SITE } from "@/lib/seo";
import { truncate } from "@/lib/utils";
export const revalidate=300; type Props={params:Promise<{slug:string}>;searchParams:Promise<{from?:string}>};
export async function generateStaticParams(){try{const slugs=await getAllPublishedSlugs();return slugs.slice(0,50).map((s)=>({slug:s.slug}))}catch{return[]}}
export async function generateMetadata({params}:Props):Promise<Metadata>{const{slug}=await params;const product=await getProductBySlug(slug);if(!product)notFound();const title=truncate(product.seo_title||`${product.name} | Mobility Station`,60);const description=truncate(product.meta_description||`${product.name}. Home and branch demonstrations from our Heathrow or Ferndown branches.`,160);const image=primaryImage(product);const absoluteImage=image.startsWith("https://")?image:undefined;return{title:{absolute:title},description,alternates:{canonical:`${SITE.url}/products/${product.slug}`},openGraph:{title,description,type:"website",url:`${SITE.url}/products/${product.slug}`,...(absoluteImage?{images:[absoluteImage]}:{})}}}
function youtubeEmbed(url:string){try{const u=new URL(url);if(u.hostname.includes("youtube.com")){const id=u.searchParams.get("v");if(id)return`https://www.youtube.com/embed/${id}`;if(u.pathname.startsWith("/embed/"))return url}if(u.hostname==="youtu.be"){const id=u.pathname.slice(1);if(id)return`https://www.youtube.com/embed/${id}`}}catch{}return null}
export default async function ProductPage({params,searchParams}:Props){const{slug}=await params;const{from}=await searchParams;const product=await getProductBySlug(slug);if(!product)notFound();const adaptation=isAdaptationProduct(product);const motabilityMode=from==="motability"&&!adaptation;const adaptationSection=adaptation&&product.category?findSectionForCategory(product.category):null;const used=isUsedCondition(product.condition);const stock=stockStatus(product);const price=displayPrice(product);const galleryUrls=[...(product.images??[]).map((i)=>i.image_url).filter(Boolean),...(product.image_url?[product.image_url]:[])].filter((v,i,a)=>a.indexOf(v)===i);const specs=Object.entries(product.specifications??{}).filter(([,v])=>v!=null&&String(v).trim());const videoEmbed=product.video_url?youtubeEmbed(product.video_url):null;const cartProduct=!adaptation&&!motabilityMode&&price.current!=null?{id:product.id,stockItemId:product.id,slug:product.slug,name:product.name,category:product.category,product_type:product.product_type,condition:product.condition,unit_price:product.unit_price,sale_price:product.sale_price,image_url:primaryImage(product)}:null;const [related,productReviews]=await Promise.all([getRelatedProducts(product,4).catch(()=>[]),getProductReviews(product.id).catch(()=>[])]);const jsonLd={"@context":"https://schema.org","@type":"Product",name:product.name,image:galleryUrls.slice(0,5),description:product.meta_description||product.description||undefined,sku:product.sku||undefined,brand:product.manufacturer?{"@type":"Brand",name:product.manufacturer}:undefined,offers:price.current!=null?{"@type":"Offer",priceCurrency:"GBP",price:price.current,availability:stock.available?"https://schema.org/InStock":"https://schema.org/OutOfStock",url:`${SITE.url}/products/${product.slug}`}:undefined};const preloadHero=galleryUrls[0]&&!galleryUrls[0].startsWith("data:")?galleryUrls[0]:null;
return <div className="pb-8 md:pb-16">{preloadHero?<link rel="preload" as="image" href={preloadHero} fetchPriority="high"/>:null}<script type="application/ld+json" dangerouslySetInnerHTML={jsonLdScript(jsonLd)}/><div className="container-site pt-5 md:pt-8"><Breadcrumbs items={[{label:"Home",href:"/"},...(adaptation?[{label:"Vehicle Adaptations",href:"/vehicle-adaptations"},...(adaptationSection?[{label:adaptationSection.title,href:sectionHref(adaptationSection.id)}]:[]),...(product.category?[{label:product.category,href:adaptationHref(product.category)}]:[])]:motabilityMode?[{label:"Motability",href:"/motability"}]:[{label:"Shop",href:"/shop"},...(product.category?[{label:product.category,href:`/shop/${product.category.toLowerCase().replace(/\s+/g,"-")}`}]:[])]),{label:product.name}]}/></div><div className="container-site pt-2 md:pt-4"><ProductDetailView name={product.name} slug={product.slug} manufacturer={product.manufacturer} category={product.category} condition={product.condition} gallery={galleryUrls} priceCurrent={price.current} priceWas={price.was} stockLabel={motabilityMode?"Available on Motability":adaptation?"":stock.label} stockAvailable={stock.available} used={used} conditionLabel={used?conditionLabel(product.condition):null} conditionGrade={conditionGradeLabel(product.condition_grade)} saleSaveLabel={!motabilityMode&&price.was&&price.current?`Sale — save ${formatGBP(price.was-price.current)}`:null} motabilityWeekly={product.motability_weekly_price} motabilityPrice={product.motability_price} adaptationId={product.adaptation_id} isAdaptation={adaptation} deliveryEstimate={product.delivery_estimate} trackStock={product.track_stock} weight={product.weight} colourOptions={product.colour_options??[]} variants={product.variants} cartProduct={motabilityMode?null:cartProduct} discontinuedMessage={product.is_discontinued?product.discontinued_message:null} description={product.description} features={(product.features??[]).slice(0,12)} suitabilityInfo={product.suitability_info} specs={specs.map(([k,v])=>[k,String(v)])} videoEmbed={videoEmbed} motabilityMode={motabilityMode}/></div>{productReviews.length>0?<div className="container-site mt-12"><ProductReviews reviews={productReviews}/></div>:null}{related.length>0?<section className="container-site mt-16 border-t border-border pt-12 md:mt-20 md:pt-16"><p className="text-xs font-bold uppercase tracking-[0.16em] text-muted">Keep exploring</p><h2 className="mt-2 text-3xl font-extrabold tracking-tight text-primary md:text-4xl">You may also like</h2><p className="mt-2 text-sm text-muted">{adaptation?"More adaptations from our catalogue.":motabilityMode?"More Motability scooters and wheelchairs.":"More scooters and wheelchairs you might consider."}</p><div className="mt-8 grid grid-cols-2 gap-4 sm:grid-cols-3 sm:gap-5 lg:grid-cols-4 lg:gap-6">{related.map((item)=>adaptation?<AdaptationCard key={item.id} product={item}/>:motabilityMode?<MotabilityProductCard key={item.id} product={item}/>:<ProductCard key={item.id} product={item}/>)}</div></section>:null}</div>}
