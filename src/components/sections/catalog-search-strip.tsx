import { SearchForm } from "@/components/layout/search-form";
export function CatalogSearchStrip({ type, title="Search the catalogue", subtitle, placeholder }: { type?:"shop"|"adaptations"; title?:string; subtitle?:string; placeholder?:string }) {
  return <section className="border-b border-border bg-soft/55"><div className="container-site py-8 md:py-10"><div className="grid gap-6 md:grid-cols-[minmax(0,0.75fr)_minmax(0,1.25fr)] md:items-center md:gap-14"><div className="max-w-lg"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-muted">Catalogue search</p><h2 className="mt-2 text-2xl font-extrabold tracking-tight text-primary md:text-3xl">{title}</h2>{subtitle ? <p className="mt-2 text-sm leading-relaxed text-muted">{subtitle}</p> : null}</div><div className="w-full"><SearchForm type={type} size="lg" placeholder={placeholder ?? (type === "adaptations" ? "Search adaptations, brands or categories" : type === "shop" ? "Search scooters, wheelchairs or brands" : "Search products, brands or adaptations")} /></div></div></div></section>;
}
