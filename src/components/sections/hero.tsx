import Link from "next/link";
import { buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";
type HeroProps={title:string;subtitle:string;primaryHref?:string;primaryLabel?:string;secondaryHref?:string;secondaryLabel?:string;compact?:boolean};
export function Hero({title,subtitle,primaryHref="/book-a-demo",primaryLabel="Book a Demo",secondaryHref="/contact?interest=callback#callback",secondaryLabel="Request a callback",compact=false}:HeroProps){return <section className={cn("border-b border-border bg-white",compact?"py-14 md:py-20":"py-16 md:py-24")}><div className="container-site max-w-4xl"><p className="text-xs font-bold uppercase tracking-[0.18em] text-muted">Mobility Station</p><h1 className="mt-4 text-balance text-5xl font-extrabold leading-[0.98] tracking-[-0.045em] text-primary md:text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-lg leading-relaxed text-muted md:text-xl">{subtitle}</p><div className="mt-8 flex flex-wrap gap-3"><Link href={primaryHref} className={cn(buttonVariants({size:"lg"}),"rounded-full px-7")}>{primaryLabel}</Link>{secondaryHref.startsWith("/")?<Link href={secondaryHref} className={cn(buttonVariants({variant:"outline",size:"lg"}),"rounded-full bg-white px-7")}>{secondaryLabel}</Link>:<a href={secondaryHref} className={cn(buttonVariants({variant:"outline",size:"lg"}),"rounded-full bg-white px-7")}>{secondaryLabel}</a>}</div></div></section>}
